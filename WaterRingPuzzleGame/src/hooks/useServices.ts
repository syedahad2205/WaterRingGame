/**
 * useServices — re-exported from Providers for convenient per-feature imports.
 *
 * Usage: import { useServices } from '../hooks/useServices';
 *
 * Requirements: 1.7, 3.4
 */
export { useServices } from '../app/Providers';
export type { Services } from '../app/Providers';
